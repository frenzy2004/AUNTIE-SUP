# Copy to .env and fill in your keys. .env is git-ignored.
# Keys are used by the offline scrape script and the dev runtime.
# In production builds, end users enter their own keys in the settings drawer (electron-store).

OPENAI_API_KEY=sk-...
APIFY_API_TOKEN=apify_api_...
EXA_API_KEY=...
